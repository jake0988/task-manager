<h1>Welcome <%= @user.username %></h1>

          <h1>All Tasks</h1>

<% if !@user.tasks.empty? %>
<% @user.tasks.each do |task| %>
<%= task.name %><br>
<% end %>
<% end %>

<%= render inline "tasks/new" %>

<%= link_to 'logout', logout_path, :confirm => "Are you sure?", method: 'delete'  %>